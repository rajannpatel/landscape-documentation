(explanation-terms-architecture)=
# Architecture

**Architecture** is the specific CPU or hardware the software has been built for. Examples include amd64, i386, armhf, arm64, s390x and source (for source packages).

